<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <!--Layout for ImageView-->
    <RelativeLayout
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:id="@+id/image_view_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Text for image."/>
        <ImageView
            android:id="@+id/image_view"
            android:layout_width="1080px"
            android:layout_height="768px"
            android:layout_below="@+id/image_view_text"
            />


    </RelativeLayout>

    <!--Layout for VideoView-->
    <RelativeLayout
        android:id="@+id/video"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:id="@+id/video_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Text for video."/>
        <VideoView
            android:id="@+id/video_View"
            android:layout_width="1080px"
            android:layout_height="768px"
            android:layout_below="@+id/video_text"/>


    </RelativeLayout>

    <!--Layout for Notes-->
    <RelativeLayout
        android:id="@+id/note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">
        <TextView
            android:id="@+id/note_text"
            android:layout_width="1080px"
            android:layout_height="768px"
            android:text="This is sample text."/>

    </RelativeLayout>

    <!--Layout for AudioPlayer-->
    <RelativeLayout
        android:id="@+id/audio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">

        <Button
            android:id="@+id/audio_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Play"
            />
        <SeekBar
            android:id="@+id/audio_seekbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@+id/audio_button"/>

    </RelativeLayout>

</LinearLayout>